[Unit]
# Prevent flightctl-agent from starting until onboarding has completed.
# This drop-in is installed by the flightctl-onboarding RPM and
# removed by the cleanup script after successful onboarding.
ConditionPathExists=/var/lib/flightctl-onboarding/.onboarding-confirmed
