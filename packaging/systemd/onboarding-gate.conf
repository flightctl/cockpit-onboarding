[Unit]
# Prevent flightctl-agent from starting until onboarding has completed.
# This drop-in is installed by the cockpit-system-onboarding RPM and
# removed by the cleanup script after successful onboarding.
ConditionPathExists=/var/lib/cockpit-system-onboarding/.onboarding-confirmed
